# 這是 Docker Compose 會自動讀取的環境變數檔案
# 建議將機密資訊 (如密碼、API Key) 放在這裡，並將此檔案加入 .gitignore 中不要進版控
# 在 docker-compose.yml 中可以用 ${DB_PASSWORD} 來讀取

DB_USER=postgres
DB_PASSWORD=SuperSecretPassword123!
DB_NAME=SampleDb
